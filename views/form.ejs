<!DOCTYPE html>
<html>
<head>
  <title><%= appointment ? 'Edit' : 'Add' %> Appointment</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">
  <h2><%= appointment ? 'Edit' : 'Add' %> Appointment</h2>
  <form method="POST" action="<%= appointment ? '/edit/' + appointment.id : '/add' %>">
    <div class="mb-3">
      <label>Patient Name</label>
      <input type="text" name="name" class="form-control" value="<%= appointment ? appointment.name : '' %>" required>
    </div>
    <div class="mb-3">
      <label>Doctor</label>
      <input type="text" name="doctor" class="form-control" value="<%= appointment ? appointment.doctor : '' %>" required>
    </div>
    <div class="mb-3">
      <label>Date</label>
      <input type="date" name="date" class="form-control" value="<%= appointment ? appointment.date : '' %>" required>
    </div>
    <div class="mb-3">
      <label>Time</label>
      <input type="time" name="time" class="form-control" value="<%= appointment ? appointment.time : '' %>" required>
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
    <a href="/" class="btn btn-secondary">Back</a>
  </form>
</body>
</html>
